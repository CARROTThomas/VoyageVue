{% extends 'base.html.twig' %}

{% block title %}BedCategory{% endblock %}

{% block body %}


    <div class="container-lg">
        <h1>BedCategory</h1>

        <table class="table">
            <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bed_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ bed_category.name }}</td>
            </tr>
            </tbody>
        </table>

        <a class="btn btn-secondary m-1" href="{{ path('app_bed_category_index') }}">back to list</a>

        <a class="btn btn-warning m-1" href="{{ path('app_bed_category_edit', {'id': bed_category.id}) }}">edit</a>

        {{ include('bed_category/_delete_form.html.twig') }}
    </div>

{% endblock %}
