<form method="post" action="{{ path('app_bed_category_delete', {'id': bed_category.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bed_category.id) }}">
    <button class="btn btn-danger m-1">Delete</button>
</form>
