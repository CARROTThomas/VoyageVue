{% extends 'base.html.twig' %}

{% block title %}New Test{% endblock %}

{% block body %}
    <div class="container-lg">
        <h1>Create new Test</h1>

        {{ include('test/_form.html.twig') }}

        <a class="btn btn-secondary" href="{{ path('app_test_index') }}">back to list</a>
    </div>

{% endblock %}
